requests>=2.34.2
python-dotenv>=1.2.2
openai>=2.44.0
jinja2>=3.1.6
markdown>=3.10.2
fastembed>=0.8.0
pyarrow>=24.0.0
numpy>=2.4.6,<2.5  # hold below 2.5: numpy>=2.5 requires Python>=3.12, but the tooling/CI baseline is Python 3.11 (see .github/dependabot.yml numpy ignore; articles #278)
Pillow>=12.2.0
PyYAML>=6.0.3  # workflow-shape tests (tools/tests/test_workflows_*.py) parse .github/workflows/*.yml; pin so importorskip never silently skips the safety rail
