name: E2E tests

on:
  pull_request:
    # Skip docs-only / roadmap-only PRs — full Chromium install + 32 browser
    # tests provide no signal on a markdown edit. Nightly cron still covers
    # main, so a docs-only PR that subtly broke a template is caught within
    # 24h. Required-check still applies on any non-doc PR.
    paths-ignore:
      - 'ROADMAP.md'
      - 'docs/**'
      - '**.md'
  push:
    branches: [main]
    paths-ignore:
      - 'ROADMAP.md'
      - 'docs/**'
      - '**.md'
  schedule:
    # Nightly at 06:00 UTC — low-noise canary
    - cron: '0 6 * * *'

permissions:
  contents: read

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          pip install -r tools/requirements.txt
          pip install pytest

      - name: Build static site
        run: python3 tools/rebuild_local.py

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install npm dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Run Playwright E2E tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      - name: Upload Playwright test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
          retention-days: 7
